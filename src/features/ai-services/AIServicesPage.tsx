import { Typography } from 'antd';

export default function AIServicesPage() {
  return <Typography.Title level={2} style={{ fontSize: 24, fontFamily: "'MTS Wide', 'MTS Text', sans-serif" }}>ИИ-сервисы</Typography.Title>;
}
