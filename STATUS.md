# STATUS — «Барометр» <!-- deploy trigger 2026-07-02 -->

## Текущее состояние

**Фаза 1 — в работе** 🟡

| Раздел | Маршрут | Статус |
|--------|---------|--------|
| AppShell + навигация | — | ✅ |
| Дашборд | `/dashboard` | ✅ |
| Метрики | `/metrics` | ✅ |
| Ассистент (ИИ-чат) | `/assistant` | ✅ |
| Воронка конверсии | `/funnel` | ✅ |
| Агенты | `/agents` | ⬜ заглушка |
| ИИ-сервисы | `/services` | ⬜ заглушка |
| Задачи | `/tasks` | ✅ (канбан, список, бэклог, таймлайн, черновики) |
| Комнаты / База знаний / Профиль | … | ⬜ заглушки |

**Стартовая страница:** `/` → редирект на `/assistant`.

---

## Ключевые решения

| Решение | Детали |
|---------|--------|
| Стек | Vite + React + TypeScript strict, antd v5 тёмная тема |
| Данные | Фикстуры через `data/api/`; реальные интеграции — фаза 4 |
| ИИ | Vercel Edge Function `api/chat.ts` — raw fetch + SSE pipe (SDK несовместим с edge-light) |
| Авторизация | HMAC-SHA256 токены (Web Crypto); логин/пароль только на сервере; `arsendsgn/481516` |
| Дев-сервер | Express :3001 + Vite proxy `/api` → :3001 |
| Стейт ИИ-панели | zustand `uiStore` — сессии чатов, `focusedMetricId`, `focusedFunnelStepId` |
| Хостинг | Vercel; preview на PR, production на `main` |
| Роутинг воронки | `users` = квартальный итог; дневная история суммируется ≈ users |

---

## Открытые вопросы

- Веса формулы скоринга бэклога (ICE/RICE + compliance + стратегия).
- Хранилище и индексация графа знаний.
- Требования банка по аудиту и хранению данных.
- Постоянное хранилище (Neon/Vercel) — нужно при реализации `/tasks`.

---

## Последние сессии

### Сессия 13 — 2026-07-02
Вкладка «Таймлайн» на `/tasks`: SVG Gantt без сторонних библиотек, стрелки зависимостей (кубический безье), drag-to-resize с каскадом, ManageDepsModal, три секции (связанные / независимые / чужие команды). Расширена модель данных: Epic, Team, `startDate`/`teamId`/`dependencies` в Task; ~40 фиктивных задач по 6 эпикам и 4 командам с кросс-командными зависимостями. AI: инструмент `get_timeline`, анализ таймлайна в промпте агента, чипы `TASK-XXX` в ИИ-панели (кликабельные, зелёные). Ветка: `claude/task-timeline-dependencies-r3n7gs`.

**Следующий шаг:** открыть PR и запросить ревью.

### Сессия 12 — 2026-07-01
Исправлена несогласованность данных воронки: дата истории → `2026-04-01` (Q2), 91 день; пики пересчитаны так, чтобы сумма дневной истории ≈ квартальному итогу тайла; сбой шага 4 перенесён на день 75 (15 июня); фильтр-бар показывает «II квартал 2026».

### Сессия 11 — 2026-07-01
`getBaseSystemPrompt()` инжектирует текущую дату; ассистент — второй пункт меню; `/` → `/assistant`, дашборд на `/dashboard`.

### Сессия 10 — 2026-06-26
Страница Воронки (`/funnel`): тайлы шагов, bar-chart общей конверсии, line-chart истории шага, тип `FunnelAnalyticsStep`, фикстура, API.
