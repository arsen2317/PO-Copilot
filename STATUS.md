# STATUS — «Барометр»

## Текущее состояние — Фаза 1 🟡

| Раздел | Маршрут | Статус |
|--------|---------|--------|
| AppShell + навигация | — | ✅ |
| Дашборд | `/dashboard` | ✅ |
| Метрики | `/metrics` | ✅ |
| Ассистент (ИИ-чат) | `/assistant` | ✅ |
| Воронка конверсии | `/funnel` | ✅ |
| Unit-экономика | `/unit-economics` | ✅ |
| Задачи | `/tasks` | ✅ (канбан, список, бэклог, таймлайн, черновики) |
| Агенты | `/agents` | ⬜ заглушка |
| ИИ-сервисы | `/services` | ⬜ заглушка |
| Комнаты / База знаний / Профиль | … | ⬜ заглушки |

`/` → редирект на `/assistant`.

---

## Фазы

| Фаза | Статус | Что |
|------|--------|-----|
| 0 | ✅ | Каркас: Vite+TS+antd, роутинг, AppShell, фикстуры, `data/api/` |
| 1 | 🟡 | Статичные экраны на моках (все разделы навигации) |
| 2 | ⬜ | Интерактив: DnD, фильтры, конструктор агентов, чат-моки |
| 3 | ⬜ | Сквозные сценарии (реакция на метрику, планирование фич) |
| 4 | ⬜ | Реальные данные: Jira, Confluence, Git, Figma, метрики |
| 5 | ⬜ | Продакшн: a11y, производительность, тесты, наблюдаемость |

---

## Экраны (спецификация кратко)

- **Дашборд** — DnD-сетка виджетов: воронка конверсии, NPS/поддержка, скорость команды, инциденты, уведомления.
- **Ассистент** — 3 колонки: список диалогов / лента сообщений / контекст. Команды `/`, упоминания `@`.
- **Агенты** — вкладки: Готовые / Мои / Конструктор (триггер → условие → действие).
- **ИИ-сервисы** — каталог сторонних инструментов с фильтрами, рейтингом, корп-подпиской.
- **Задачи** — Канбан / Список / Бэклог (авто-скоринг ICE/RICE+compliance) / Таймлайн (SVG Gantt).
- **Комнаты** — типы: Планирование спринта / Ретро / Груминг. Список → детальная комната.
- **База знаний** — граф артефактов (задача, PR, метрика, Confluence, исследование) + поиск.
- **Уведомления** — выпадающая панель из хедера + `/notifications`.
- **Профиль** — вкладки: Уведомления, Интеграции, Персонализация, Агенты/сервисы, Команды.

---

## Ключевые решения

| Решение | Детали |
|---------|--------|
| Стек | Vite + React + TypeScript strict, antd v5 тёмная тема |
| Данные | Фикстуры через `data/api/`; реальные интеграции — фаза 4 |
| ИИ | Express API `scripts/dev-api-server.ts` на порту 3001; SSE стриминг через Anthropic SDK |
| Авторизация | HMAC-SHA256 токены (Web Crypto); логин/пароль только на сервере; `arsendsgn/481516` |
| Хостинг | VPS Timeweb (72.56.34.107), nginx + PM2 + certbot |
| CI/CD | GitHub Actions: build → SCP dist/ → SSH restart PM2; push → main |
| Прокси | Cloudflare Worker `anthropic-proxy.arackelian.workers.dev`; репо `arsen2317/anthropic-proxy` |
| Роутинг воронки | `users` = квартальный итог; дневная история суммируется ≈ users |

---

## Открытые вопросы

- Веса формулы скоринга бэклога (ICE/RICE + compliance + стратегия).
- Хранилище и индексация графа знаний.
- Требования банка по аудиту и хранению данных.
- Постоянное хранилище (Neon/Vercel) — нужно при реализации `/tasks`.

---

## Последние сессии

### Сессия 20 — 2026-07-03
4 UI-правок:
1. **Sidebar AuroraGlow** — исправлен root cause: убран `overflow: hidden` с контейнера, контейнер теперь перекрывает всю высоту панели (`top: 0, bottom: 0`). Блобы позиционированы на `bottom: '20-22%'` с blur 70-85px — aurora видна на 30-40% высоты панели. Hover-анимация работает корректно.
2. **Unit-экономика: пай-чарт легенда** — отключена встроенная легенда (`legend={false}`), добавлена кастомная центрированная легенда (flex, gap 18) с цветными квадратиками под чартом.
3. **Unit-экономика: вертикальное центрирование вкладок** — добавлен `className="ue-charts-tabs"` на Tabs + CSS в `global.css` для пробрасывания `display: flex, flex: 1` через `.ant-tabs-content-holder / .ant-tabs-content / .ant-tabs-tabpane`. Пай-чарт теперь вертикально центрирован.
4. **База знаний — детальная страница** — заголовок перестроен в одну строку (`← | название | тег | дата`); убран `maxWidth: 760`; добавлен двухколоночный layout: контент (`flex: 1`) + AI-панель справа (240px) с 3 быстрыми промптами, кнопки активируют глобальный ИИ-ассистент через `useUIStore.setPendingAgent/setPendingTrigger`.

**Следующий шаг:** продолжить разработку заглушек — агенты, сервисы, комнаты, профиль.

### Сессия 19 — 2026-07-03
6 UI-правок:
1. **База знаний** — поиск по названию/содержимому (Input с allowClear) + фильтр по типу артефакта (Select). Пустое состояние при отсутствии результатов.
2. **База знаний — детальная страница** — поле `sourceUrl?: string` в `KnowledgeArtifact`; в фикстурах заглушки; в `ArtifactDetailPage` показывается блок «Источник» со ссылкой (если задана).
3. **Unit-экономика — левая колонка** — убрано `justifyContent: space-between`, заменено на `gap: 20` между секциями доходов/расходов/LTV. Скролл вместо overflow: hidden.
4. **Unit-экономика — пай-чарты** — контент вкладок «Структура выручки» и «Структура расходов» теперь центрирован (flex column + alignItems center), чарт ограничен maxWidth: 420.
5. **Ассистент GlowBg** — блобы переработаны: все три перекрываются в нижнем центре (left/margin вместо right:-50/left:-40), больший размер, увеличен blur.
6. **Sidebar SidebarAuroraGlow** — высота уменьшена с 55% до 38%; блобы перепозиционированы на центр (26%, 50%, 68%); добавлен `hovered` prop — opacity увеличивается с плавным transition 0.5s при наведении на панель.

**Следующий шаг:** продолжить разработку заглушек — агенты, сервисы, комнаты, профиль.

### Сессия 18 — 2026-07-03
CJM Агент:
- **uiStore**: добавлен `pendingAgentKey` / `setPendingAgent` — для запуска агента из внешних страниц
- **tools.ts**: 5 новых инструментов — `get_cjm_list`, `get_cjm`, `get_knowledge_artifacts`, `create_cjm`, `update_cjm`
- **agentPrompts.ts**: полный промпт `agent-cjm` с 3 режимами (создание/актуализация/редактирование), правилами позиций нод и требованиями к качеству
- **AIPanelSider**: новый агент в карусели (aurora teal), обработчик `cjm-result` блока (кликабельная карточка), навигация `__cjm__`, `useEffect` для `pendingAgentKey`
- **CjmListPage**: кнопка «Создать CJM с ИИ» активирует CJM агент в правой панели

Агент умеет: собирать метрики + воронку + базу знаний + web-поиск → генерировать полный CJM → сохранять в сессии → показывать кликабельную карточку с переходом на canvas.

**Следующий шаг:** продолжить разработку заглушек — агенты, сервисы, комнаты, база знаний, профиль.

### Сессия 17 — 2026-07-03
5 UI-правок:
1. **Поиск** — SearchModal заменён на реальный поиск по 12 разделам приложения с фильтрацией по лейблу/описанию/ключевым словам и навигацией.
2. **Градиент в правом панели** — `GlowBg` в sidebar-режиме заменён на `SidebarAuroraGlow` с тремя анимированными блобами (размер больше, чем на карточках агентов).
3. **Divider над input** — убран горизонтальный разделитель между строкой «Автоматизировать» и textarea в `InputCard`.
4. **CJM модальные окна** — добавлен `centered` к обоим модалкам в `CjmEditDrawer` (редактор ноды + AddStageDrawer).
5. **Unit Economics** — левая колонка перестроена с `justifyContent: space-between` (три секции разносятся по высоте); Pie-графики увеличены до 320px; убраны двойные bottom spacer'ы с Dashboard, Funnel и UnitEconomics.

**Следующий шаг:** продолжить разработку заглушек — агенты, сервисы, комнаты, база знаний, профиль.

### Сессия 16 — 2026-07-03
Cloudflare Worker прокси починен: `arsen2317/anthropic-proxy` был инициализирован шаблоном «Workflows Starter» (без кода прокси). Написан реальный `worker/index.ts` (роуты `/anthropic/*` → Anthropic, `/brave/*` → Brave Search), упрощён `wrangler.jsonc`, добавлена миграция `deleted_classes: WorkflowStatusDO`. Ключевое: `ANTHROPIC_PROXY_URL` имеет суффикс `/anthropic`, `BRAVE_PROXY_URL` — `/brave`; Worker стрипает префикс перед форвардингом. PR #3 смержен. CLAUDE.md, STATUS.md сжаты, BAROMETER.md удалён.

**Следующий шаг:** продолжить разработку заглушек — агенты, сервисы, комнаты, база знаний, профиль.

### Сессия 15 — 2026-07-03
Unit-экономика (`/unit-economics`): калькулятор дебетовой карты. Двухколоночный layout: ввод (доходы/расходы/LTV) + KPI-плитки + графики (SVG окупаемость, Pie структура выручки/расходов). Формулы: Revenue, Cost, CM, Payback, LTV (с дисконтом и churn), LTV/CAC.

### Сессия 14 — 2026-07-02
Миграция с Vercel на VPS (Timeweb). nginx + PM2 + certbot. GitHub Actions CI/CD. Cloudflare Worker прокси для обхода блокировки РФ.
